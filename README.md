# Spam-Detection

This is Spam Detection Model.
This code implements a complete Natural Language Processing (NLP) pipeline to classify emails as either "Spam" or "Ham" (not spam). It moves from raw data to a trained Deep Learning model.

